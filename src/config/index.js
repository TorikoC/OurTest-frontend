const host = 'http://localhost:3001/api';

export default {
  comments: `${host}/comments`,
  login: `${host}/login`,
  users: `${host}/users`,
  tests: `${host}/tests`,
  sendmail: `${host}/sendmail`,
};
